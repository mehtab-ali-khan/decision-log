## Original Prompts

These are the raw prompts I gave the AI. Note that this file is generate by ai so that is readable but the prompts itself are exactly what I used. Prompt 27 & 28 were generated by ai (I asked claude to give me prompt using best practices to improve ui/ux of app) not replaced by me after developement. 

### Models Used

- Prompts 1–16: Claude Sonnet 4.5 (browser)
- Prompts 17–32: GPT-5.5 (Tera / Luna) in Codex
- Prompts 33–50: Claude Opus 5 in Codex
- Prompts 51–61: GPT-5.5 (Luna) in Codex


## Prompt 1
```
extraxt important client breifs from this project and give me in bullet points.
```

## Prompt 2
```
give only requirements of the actual app nothing else regarding assigment deployment landing page etc.
```

## Prompt 3
```
i am confused whether we will create actual projects first or just plain decisions like what do you mean by mange or iterect with them? if there is anything uncertain and confusiong in the documents lets clerfiy them first so ask me questions if any.
```

## Prompt 4
```
1. project vise decisions, keep project data minimal like just name of project which will be populated by default like project1 , project2 etc, when user opens at decsion page there will be project field which will be a hybrid of select/text , if no projects exist yet there will be default project1 option in dropdown , or if project1 is already created so next time when user add decsion the default project text will be project2 and project1 will also be shown in dropdown and so on.
2. edit, delete, toggle active/inactive status, filter/search.
3. active just be a manual toggle
4. list on desktop view and card on mobile view with pagination with all fields visible to start off with.

any question/confusions now?
```

## Prompt 5
```
1. 10
2. simple single searchbar that will search over all fields plus a  filter on status and project
3. yes plus also shows custom project names in the list
4. project changeable
5. confirm step required
6. newest first
7. empty states

any confusion/question now? dont build the scope yet i need to add some other things if u are good till now
```

## Prompt 6
```
i also want

1. custom api hooks for all data lisitng/modification. i mean dont just randomly pull data from other file rather make dummy api calls which will return data from file with some delay so that later when we add actual backend we just have to swipe the dummy apis. are you clear on this point?
2. i want skeleton while loading, toast msgs , alerts on delete
3. i have 2 questions , were should we store data what options we have, which exact stack/libraries options we have?
4. which library we use for toast msg/icons/styling?
5. i want a single config file for app theme.

this repo will hold both the landing page and the actual app. i also want to use typescript plus do some basic seo so that the preview of link looks good. forget landing page for now it should just have try now btn that should take us to actual app.
```

## Prompt 7
```
3. react usestate / usereducer, i am skipping landng page for now but i will later make landing page content-heavy/static-optimized, should we use react astro or something else what about next js
4. react hot toast, lucide react, tailwind css, react router dom
```

## Prompt 8
```
ok lets go with React + Vite + TypeScript, react-router-dom,  lifted useState/useReducer(no third party api), react-hot-toast, lucide-react, Tailwind CSS.
give me shortest and simplest plan , do not intrduce anything extra, write simplest code while using best practices (list best practices to follow specificaly), do not write code for anything that does not exist or we faced yet , no extra comentry or comments no extra files functions classes.
i want a plan to be in standalone phases that i can review/commit incrimently.
```

## Prompt 9
```
can u give me separate readme, agents, roadmap.md files
```

## Prompt 10
```
what about styling theme branding color have we specified/decide them in these files?
```

## Prompt 11
```
1. Modern saas 
2. primary color plus neutral grey
3. fast, neutral, less "AI-generated"
4. idk what are your suggestions i want the ui to be clean minimal and modern the ux should should be good enough for non technical persons
```

## Prompt 12
```
looks good just add very minimal pleasent animations and update our files/
```

## Prompt 13
```
i dont want  — and other similar kind of ai shits , i dont want any of single file/line in my repo to look like ai generated
```

## Prompt 14
```
give instruction so ai only write code, i dont want it to run any command or add any test,
dont update the file give me instructions here.
```

## Prompt 15
```
thats an overkill just give me 2-3 points.
```

## Prompt 16
```
give gitignore
```

## Prompt 17
```
impliment phase 0
```

## Prompt 18
```
impliment phase 1
```

## Prompt 19
```
looks good now impliment phase 2
```

## Prompt 20
```
impliment phase 3 now
```

## Prompt 21
```
`i`mpliment phase 4 now
```

## Prompt 22
```
styling are not applied.
```

## Prompt 23
```
ok now impliment phase 5
```

## Prompt 24
```
`i`mpliment phase 6 now
```

## Prompt 25
```
`i`mpliment phase 7 now
```

## Prompt 26
```
review decisionslog.tsx and split it in appropriate components , keep it simple dont over engineer it.
```

## Prompt 27
```
The current UI does not match the theme tokens in `AGENTS.md`/`README.md`. Fix the following:

- Apply the theme consistently: primary `#2450E0`, neutral grays, `6px` border-radius, system font stack, on every input, button, and card. Right now it looks like unstyled default browser form elements.
- Style native inputs (date picker, checkbox, textareas) to match the theme instead of using browser defaults.
- Group Search, Status, and Project filters into a single inline toolbar row on desktop instead of three full-width stacked fields.
- Give the decision list a proper surface treatment: card container, consistent row height, subtle row hover state, one border color from the theme.
- Separate the create/edit form from the list visually. Use a modal or slide-over panel instead of stacking the form directly above the list on the same page.
- Establish button hierarchy: primary actions filled with the primary color, secondary actions (Cancel, Previous, Next) as outlined/ghost buttons, not visually equal weight to primary.
- Add visible focus states on inputs using the primary color, and consistent input height/padding across all fields.
```

## Prompt 28
```
Act as a senior UI/UX designer reviewing my app.

I want a **clean, modern, polished, eye-catching UI/UX**—not an outdated CRUD interface built around basic forms, tables, borders, and generic components.

Review the app critically and identify:

* UI/UX problems
* Outdated or weak design patterns
* Poor visual hierarchy
* Clutter, spacing, alignment, and typography issues
* Confusing or inefficient user flows
* Places where forms/tables should use better modern UI patterns
* Inconsistent components or styling
* Missing states or feedback
* Responsive/mobile issues
* Anything that makes the product feel unpolished or amateur

### Output rules

Give me **only a prioritized bullet-point list of fixes**.

For each bullet, use this format:

**[Area] — Problem → Fix**

Keep each point concise but specific enough that I can later ask you to implement that particular fix.

Do **not** implement anything yet.
Do **not** provide code.
Do **not** give long explanations.
Do **not** suggest generic advice like "make it modern."

Be critical and opinionated. Focus on the changes that would make the biggest difference to the product's overall UI/UX.
```

## Prompt 29
```
this is just a poc so skip the once that are over engineered here and fix rest of all , skip dark theme for now. just update the code do not run any command or tests.
```

## Prompt 30
```
i am short off time and tokens do not run any long running commands just complete this asap
```

## Prompt 31
```
Demo · data resets on refresh , remove this label instead add a add dummy data btn that should add around 15 items and by default it should be no data.
remove n icon from new decision btn
use modal at center of screen for the add decision form
delete should show confirmation modal instead of undo
remove this text also.
All decisions
What your team decided, why, and whether it still stands.
just fix these dont run any command  do not do extra work
```

## Prompt 32
```
simplify the code where possible remove all kinds of commentry use simplest english for placholder texts also remove — this and other similar kinds of ai genertaed lables i dont want my repo/app to look like ai generated in any kind.
dont run extra command and test and skip landing page for now.
```

## Prompt 33
```
Review the project to build your contexnt. don't explain back to me. 
```

## Prompt 34
```
ok, I want you to work on just the landing page. I want landing page to be 

A convincing landing page (more important that product itself).
Explain what the product does
Make it feel like a real product someone might use
You are allowed to over-promise since its a marketing page
Include a prominent Try Demo action
The landing page should communicate clarity, confidence and momentum, feel credible to founder


As for styling and ui, I don't want it to look like ai generated and old look, Rahter it should look like modern landings pages like stripe https://superhuman.com/  & https://stripe.com/ . for images use @Screenshot 2026-09-16 at 3.14.15 AM (1).png [image attached] @Screenshot 2026-09-16 at 3.14.15 AM.png [image attached] . for demo video keep placholder, I wil  add one. keep code simple, dont over engineer it. dont run any commands or tests. Just do the code chanbges.
```

## Prompt 35
```
move the demo video to top hero section. make the clients list more appelaing. maybe use some animations there and use their oringal logo instead of plane text. I don;t like the Why it works cards deisng (too old). for two screenshots, use them togheter in Capture section.
```

## Prompt 36
```
I want hero section to be somehting like superhuman. see attached screenshot for reference
```

## Prompt 37
```
I dont liek the background of right section. also the button is too common. and hter eis noe right spacing for the demo video
```

## Prompt 38
```
use very light font weight for all the headings on landing page like in screenshot. Also rempve the  gradiant from CTA button.
```

## Prompt 39
```
ok use @one.png [image attached] & @two.png [image attached] for images and move them to approcpriacte folder.
```

## Prompt 40
```
I also have added @three.png [image attached] 
```

## Prompt 41
```
no, use it in same place as with other two
```

## Prompt 42
```
it does not look good. act like a senior ui ux deisnger to came up with something good
```

## Prompt 43
```
ok looks good, use this url for demo viedo: https://www.loom.com/share/be7c44c601334530a35d87c129e9c2a9 
and our landing page looks small. lets add one more section. these were the oring al requirements:
Explain what the product does
3.
4.
5.
6.
Make it feel like a real product someone might use
You are allowed to over-promise since its a marketing page
Include a prominent Try Demo action
The landing page should communicate clarity, confidence and momentum, feel credible to founders
and product teams, and make the value of preserving important decisions immediately obvious
```

## Prompt 44
```
use simplest english for placehodlers to landing page does not look like ai generated. avpid hyphesna and other common ai slogans. 
```

## Prompt 45
```
lets add some minimal animations so it looks beautifuly. don't overkill it. just minimal. And lets swao the image from one to two as soon as user comes to How it works section. and also use this embod code for demo vidoe instead of pain url:

<div style="position: relative; padding-bottom: 62.5%; height: 0;"><iframe src="https://www.loom.com/embed/be7c44c601334530a35d87c129e9c2a9" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen style="position: absolute; top: 0; left: 0; width: 100%; height: 100%;"></iframe></div>
```

## Prompt 46
```
lets add a dummy pricing section. 
```

## Prompt 47
```
the most popular card should have a larget hiehgt or omething extra as we often see. alos use someoringal image for Priya in tetimonoial maybe: https://mui.com/static/images/avatar/2.jpg 
```

## Prompt 48
```
lets also move product image to three after sma time we moev from one to two on section arrival. 
```

## Prompt 49
```
Lets also build a contact us page. and add fame social media icons at footer. 
```

## Prompt 50
```
the contact us should also live in landing page and cusotm plan CTA should take it to contact form
```

## Prompt 51
```
review the code changes and remove any kind of extra comments or commentry , simplify the code if anywhere possible.
```

## Prompt 52
```
the app link url does not showing image preview add decision-search.png from assests for previews if possible .
```

## Prompt 53
```
ok the project is complete and lets move to the last step , deploying to gihub pages , take me with simple step by step actions to deploy and explain each step , do not add any type of extra work.
```

## Prompt 54
```
https://mehtab-ali-khan.github.io/decision-log/ giving route errors.
whats the problem in routes?
```

## Prompt 55
```
the link preview does not showing image preview and title description also fix them. 
```

## Prompt 56
```
the app mobile view is broken in some parts , like add decision modal open with no spacing from top bottom , main app page navbar is broken also  like logo and app name is not showing in one line maybe should small the size of sample and new btn or remove their icons if neccesry, fix the moible view any broken ui.
```

## Prompt 57
```
keep the image simple now just app logo and name in center with a little size and  benreth it will be the subtext keep a record of what your team decided and why just with a bigger size and bold, remove card and paragrapgh other things keep it profesianl.
```

## Prompt 58
```
why this happen if i not come from / page the app url not works , explain first and then fix this.
```

## Prompt 59
```
on how it works section images changes in 1-2 second delay and stops after 3rd image, kep it changes from 1-2-3 again 1-2-3  in 1-2 seocnd delay.
```

## Prompt 60
```
when i clcik on landing page btns of how it works,products,demo etc /#demo this comes up with 404. fix it
```

## Prompt 61
```
i added loom demo video via emebed code , but it load take 10-15 seconds can we improve this ?
```